# `fixtures/` — plans for MANUAL checks

**These are not the corpus.** `examples/` holds reference plans: they are
validated by `tests/test_schema.py`, frozen by `tests/test_corpus_freeze.py`,
and changing one needs a declared justification. Nothing here is any of that.

A file in this directory exists to be **opened by a human during a manual
check** — the AMBER gate at the end of a task. It may be deliberately dirty,
because the state a check needs to start from is often exactly the state the
invariants exist to complain about.

**Why it is not in `examples/`, stated once so it is not re-litigated.**
`tests/test_schema.py` parametrizes over a **filesystem glob** of
`examples/*.json`, so a plan dropped in there changes the collected test count
and — if it trips an invariant — turns the gate red and, through the commit
hook, **blocks every commit in the repository**. That is
[D51](../docs/defects/0051-the-census-depends-on-the-working-tree.md), and it
happened to `fragment2room.json` on 2026‑08‑08 before it was moved here.
This directory is outside both corpus tests by construction.

**So a file here may be edited freely — UNLESS a test names it.** No freeze, no justification, no re-cut for an ordinary check plan. **But a fixture a test depends on is load-bearing**, and the table below says which, and why. A plan retained *because* it is dirty carries a guard test that fails when the dirt is cleaned: without one, a good-faith repair leaves the dependent test green while it exercises nothing.

| file | what it is for |
|---|---|
| `wiscaway2026-08-08.json` | **Patrick's own plan**, moved here from `examples/` on 2026‑08‑08 because it is a working drawing, not a reference. It trips **one** invariant — `I7  opening o29 runs off wall w90 (68.0..116.0 of 72.0)`: a 48″ pocket door anchored 68″ along a 72″ wall, straddling the welded junction at `v90` so 4″ sits on `w90` and 44″ on `w91`. **That fault is the trigger for [D57](../docs/defects/0057-face-at-hands-walls-of-a-report-of.md)** — deleting a room and re-naming the enclosed space with the Room tool crashes the app, on this tree and on `main`. **RETAINED BECAUSE IT FAILS.** The door is what drives `_walls_of` down its `if straddles:` branch, and **three synthetic scenes were tried and none reached it** — `rebuild_all_walls` re-seats a hand-built straddler. So this plan is the only input the D57 test has. **Do not repair the door.** `tests/test_design_bridge.py::test_the_wiscaway_fixture_is_still_dirty_in_exactly_the_way_that_matters` fails if you do — the same shape as `KNOWN_UNCLEAN`, written here because `fixtures/` sits outside the corpus tests by construction. |
| `wiscaway2026-08-09R.json` | **Patrick's own plan, one day later and with SHUFFLE OFF** — the same drawing as `wiscaway2026-08-08.json` after a session of ordinary work. It is the field evidence for [D61](../docs/defects/0061-a-room-move-permanently-adds-two-walls.md): walls **103 → 134**, vertex objects **101 → 201**, outline corner slots **159 → 241**, redundant-looking corners **69 → 129**. Shuffle off is why — under shuffle a label-drag leaves the room floating and `join_room` never runs, so D61's producer never fires. **RETAINED BECAUSE IT FAILS**, on seven referential invariants: `I7 opening o26 runs off wall w68 (48.3..108.3 of 95.5)` · `I7 opening o27 runs off wall w75 (20.8..52.8 of 44.8)` · `I7 openings o36/o37 overlap on w126` · `I11 rooms 'Dining' and 'Front Porch' overlap` · `I14 wall w87 end v92 lies on wall w85 but is not a vertex of it (unwelded T)` · the same for `w90/v92` · the same for `w116 end v148` on `w109`. It is also the only plan measured on which the outline coalesce's **angular** path dominates — `24 exact / 60 by angle`, against `28 exact / 0 by angle` on the 08‑08 file — which is what turned "no room's area moves" from a property of the operation into a property of that one plan. **Do not repair it.** `tests/test_design_bridge.py::test_the_2026_08_09R_fixture_is_still_dirty_in_all_seven_ways` fails if you do. |
| `fragment2room.json` | **D47 / A1's manual check.** Two overlapping placed rooms, `A` (255.8 sf) and `B` (249.8 sf), overlapping by **81.0 sf** — the input `Rooms ▸ Fragment into pieces` exists to resolve. It trips `I11` **on purpose**: the overlap is the fixture. Fragmenting it gives three disjoint pieces totalling 424.5 sf, and that 505.5 → 424.5 drop is itself evidence the pieces do not overlap. Also the plan behind [D53](../docs/defects/0053-a-room-cannot-be-selected-by-clicking-its.md)'s click differential. |
| `chief-export/sample_design.json` + `L1.dxf`/`L2.dxf` + `L1.openings.json`/`L2.openings.json` | **`fp2dxf`'s golden-file pair — [`handoff/0038-ruling.md`](../docs/handoff/0038-ruling.md) §7.** A two-storey design exercising doors (hinged + slider), windows (v1/center anchors), a railing + gate, an open edge and a concept room, landed with the handoff and placed here rather than `examples/` (a manual-check input paired with an exporter, not a schema-corpus member — D51). `L1.dxf`/`L2.dxf`/sidecars are `convert()`'s CURRENT output, **regenerated** from the originally-shipped pair after the thickness table was rewired to read `STD_T` live (§2–§3 of the ruling): only `exterior` (6.5″→6.0″) and `railing` (3.0″→2.0″) wall faces moved; `interior`/`partition` and both sidecars are unchanged — full accounting in `tests/test_fp2dxf.py`'s module docstring. **Load-bearing**: `tests/test_fp2dxf.py` asserts `convert(sample_design.json)` reproduces `L1.dxf`/`L2.dxf`/both sidecars content-identical (text-compared, not raw bytes — `convert()`'s own writer is platform-dependent on line endings; see the test module's docstring) — a thickness change, a layer rename or a coordinate flip turns it red, so regenerate deliberately and update that docstring's diff, never silently. The 16-screenshot verified Chief Architect import workflow this sample was proven against lives at [`docs/evidence/chief-export/`](../docs/evidence/chief-export/) and the root [`README.md`](../README.md#export-to-chief-architect-dxf). **Patrick's own manual check passed 2026‑08‑17** — exported, imported into Chief Architect X17 — closing [`0038-ruling.md`](../docs/handoff/0038-ruling.md) §8's merge condition. |
| `crossfloor-snap-2026-08-17.json` | **Patrick's own plan, promoted from `incoming/` — [`handoff/0061-ruling.md`](../docs/handoff/0061-ruling.md).** A real two-storey design (`L1` 106 walls, `L2` 45), the report behind the cross-floor snap-through investigation ([`0035`](../docs/handoff/0035-ruling.md)–[`0037`](../docs/handoff/0037-ruling.md), [`0061`](../docs/handoff/0061-ruling.md)) and, independently, the corpus's worst orthogonality outlier (36 of its walls over 5° — [`0058-report.md`](../docs/handoff/0058-report.md)/[`0060-report.md`](../docs/handoff/0060-report.md)). **No test names it** — the cross-floor defect it was dropped for was found and reproduced by a minimal SYNTHETIC scene instead (`tests/test_floors.py::test_align_to_wall_does_not_snap_to_a_hidden_floor`; `PlanView._align_to_wall` scanned `scene.items()` with no `.floor` filter), which is deterministic and far smaller than this 151-wall plan, so the promotion exit is *"deleted as covered by the synthetic case"* for the DEFECT and *"promoted, no test names it"* for the FILE itself — kept as real corpus evidence (`docs/evidence/orthogonality_census.py` sweeps `fixtures/` and counts it) rather than deleted outright, per [`0061`](../docs/handoff/0061-ruling.md) §6's either-way naming. Carries no `.txt` note; none was ever supplied across five handoffs. |
| `w7offgrid.fpm` + `w7offsetFloorplan.json` | **Patrick's own reproduced report, promoted from `incoming/` under exit 1 — [`handoff/0070-ruling.md`](../docs/handoff/0070-ruling.md), [D80](../docs/defects/0080-a-fresh-wall-started-at-a-t-junction-inherits.md).** A 14-step macro replaying interior-wall draws and slides at `wall_snap_in = 6.0`; two of its produced walls' T-junction vertices land off the 6″ grid — `PlanView._snap_start` returned a T-junction body-projection verbatim, with no grid-snap. **Load-bearing**: `tests/test_walls.py::test_w7offgrid_macro_lands_every_vertex_on_the_snap_grid` replays `w7offgrid.fpm` on a blank canvas and asserts every vertex it produces is congruent to `0 mod wall_snap_in` — confirmed RED against the unfixed code, GREEN after (D80's own receipt). `w7offsetFloorplan.json` is the real-world saved artifact the drift was first noticed in; no test loads it directly (the invariant is about what the app *produces*, not the corpus of legitimately off-grid loaded history — `0070-ruling.md` §5 item 3), so **do not repair its coordinates**: they are the evidence. |
| `shower-glance-check.json` | **The three-enclosure redraw's fail-first pair — [`handoff/0030-ruling.md`](../docs/handoff/0030-ruling.md) / [`0031-ruling.md`](../docs/handoff/0031-ruling.md) / [`0034-ruling.md`](../docs/handoff/0034-ruling.md).** `shower` / `walk_in_shower` / `glass_shower`, left to right, in a plain walled room. **TWO CAMERAS, both on the record because they answer different questions ([`0034`](../docs/handoff/0034-ruling.md) §2):** the ROOM-SCALE camera (`python floorplanner/viewer/fp3d.py fixtures/shower-glance-check.json --shot <path>`, `make_view`'s own auto-fit over the whole 348in × 138in model, walls included) — [`docs/evidence/shower-glance-before.png`](../docs/evidence/shower-glance-before.png) / [`-after.png`](../docs/evidence/shower-glance-after.png) — is the COMPARABILITY camera `0031` fixed, but it is not the distance a person actually works at (D74's own lesson, applied here). The WORKING-DISTANCE camera (`python docs/evidence/shower_glance_working_distance.py`, re-points the pivot and closes the distance to the FURNISHINGS' own bounding box rather than the room's) — [`docs/evidence/shower-glance-working-distance-before.png`](../docs/evidence/shower-glance-working-distance-before.png) / [`-after.png`](../docs/evidence/shower-glance-working-distance-after.png) — is the one Patrick's glance-test question is actually about. **DO NOT EDIT THIS FILE before an after-shot is taken, at either camera.** No test names it, but the manual check does: a before/after pair whose input changed between the two proves nothing, and proves it silently. |
| `wiscaway2026-08-30R1.json` | **Patrick's own plan, promoted from `incoming/` under exit 1 — [`handoff/0127-ruling.md`](../docs/handoff/0127-ruling.md) (authored as `0124`, renumbered on landing — see that file's own note).** `L1` 148 walls across 33 rooms, a diagonal wing (`SUN`/`OFFICE`/`HALL2`/`UTILITY`/`MUD`/`GARAGE`) off the right side at ~45°/~135°. The reproduction for the angled-dimension-lane's first cut's three faults, screenshotted with `Show dimensions` on for that wing at [`docs/evidence/DIM45.png`](../docs/evidence/DIM45.png): door positions listed as stations, the lane sized off the whole plan bbox instead of the wing's own footprint, and extension lines crossing straight through the wing to reach it. **Load-bearing**: `tests/test_fp2pdf.py::test_angled_lane_on_the_real_wiscaway_wing_stays_snug_and_off_the_geometry` renders this file with those rooms' `show_dimensions` forced on and asserts the fixed geometry — no station beyond wall endpoints, the lane's reach sized to the wing not the house, every member wall inside that reach — confirmed RED against the pre-fix code, GREEN after. |
| `wiscaway2026-08-30R2.json` | **Patrick's own plan, promoted from `incoming/` under exit 1 — [`handoff/0132-ruling.md`](../docs/handoff/0132-ruling.md), superseding `R1` as the angled-dimension tranche's check file.** `L1` 146 walls, 33 rooms, **15 rooms with `show_dimensions` already on** (no forcing needed), `wall_snap_in: 6.0` in the document's own settings; `L2` present too — the first two-level wiscaway fixture. The before-export is [`docs/evidence/wiscaway2026-08-30R2g.pdf`](../docs/evidence/wiscaway2026-08-30R2g.pdf) (pre-tranche code, `R2g` in Patrick's own naming). **Load-bearing**: `tests/test_fp2pdf.py::test_real_wiscaway_r2_wing_vertex_is_absent_from_the_orthogonal_rows` asserts `0130-ruling.md`'s family-exclusivity rule against this real file — an angled-wing wall's own vertex must not appear in the X or Y row's stations. `docs/evidence/pdf_dimension_telescoping_census.py` also walks it for the corpus-wide grid-filter and per-family telescoping receipts (`0129-ruling.md` §3(a), `0130-ruling.md` §2). |
| `disappearingroof.fpm` | **Patrick's own reproduced report, promoted from `incoming/`.** A 6-line macro: four walls closing a rectangle, `G CLICK ... DRAG ...` sketching a ridge across it, then `S ^Z` — switch to Select, then Undo — with **no eaves-pick click in between**. That is the whole bug: the ridge is already released and rendered with a full eave/gable preview, so it reads as a finished roof on screen, but `PlanView` still considered it mid-gesture and `cancel_temp()` (fired by the tool switch) silently discarded it — "the roof disappears," in his own words. **Load-bearing**: `tests/test_roof_ridge_tool.py::test_replaying_the_reported_macro_keeps_the_roof_after_switching_tools` replays this file verbatim and asserts a roof survives the `S` step (auto-completed via the same nearest-wall search a loaded roof already uses to re-derive `span_in`) and that `^Z`/redo round-trip it cleanly — confirmed RED against the unfixed `cancel_temp()`, GREEN after. |
| `roofs-r3-orbit-check.json` | **Synthetic, built for R3's own evidence shot, not a real Patrick plan.** Two disconnected rectangular masses on one level: a 360×240 house with an orthogonal ridge (heights 96/150, 12″ overhang) and a 120×80 wing rotated 45° with its own ridge (heights 96/132, 8″ overhang) — the "45° wing carries its own ridge" claim ([`0139-ruling.md`](../docs/handoff/0139-ruling.md) sec2), rendered rather than merely asserted. No test names it; it exists so `python floorplanner/viewer/fp3d.py fixtures/roofs-r3-orbit-check.json --shot <path>` (real display required — D77/D78) reproduces [`docs/evidence/roofs-r3-planes-gables.png`](../docs/evidence/roofs-r3-planes-gables.png). May be edited freely. |
