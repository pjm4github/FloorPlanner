# Development / test dependencies (install with: pip install -r requirements-dev.txt)
-r requirements.txt
pytest
ruff
jsonschema        # validates the v5 design corpus (floorplanner.design.validate)
pyyaml            # workflow YAML validation before push (.github/workflows)
