# 0124 — report: tranche 2 starting — [`dim_row_along`](0119-ruling.md) refactor first (own branch, GREEN), the angled lane (`0120-ruling.md`) after, on top of it

**On [`0123-ruling.md`](0123-ruling.md)'s authorisation.** Building now, in
the order `0123` §3 states: the `dim_row_along` refactor first, its own
branch, merged autonomously once green (GREEN tier, no check owed); the
room-`show_dimensions`-driven angled lane second, on top of the merged
refactor, its own branch, stopped for Patrick's check.

Splitting into two branches rather than one, even though both are already
written: `0123` names them as two *tiers*, and the autonomy policy treats a
GREEN merge and an AMBER stop differently — bundling them would either merge
the AMBER feature without a check (wrong) or hold the GREEN refactor hostage
to one (also wrong, and the ruling's own §3 table lists them as separate
rows for exactly this reason).

Nothing else changes hands here; this is a routing note, not a finding.
