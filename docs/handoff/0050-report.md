# 0050 — report: `fp2dxf` integration built end to end, PR #33 open, AMBER

**Per [`0038-ruling.md`](0038-ruling.md) §§5-8, [`0044-ruling.md`](0044-ruling.md)
§3 item 6 and [`0047-ruling.md`](0047-ruling.md) §5** — the DXF integration,
on a fresh context, as ordered. Branch `fp2dxf-integration`,
[PR #33](https://github.com/pjm4github/FloorPlanner/pull/33), pushed, not
merged.

---

## 1. WHAT WAS DONE, against §5-§8's owed list

**§5 — placement.** `docs/handoff/0038-fp2dxf-handoff.zip` unpacked and
deleted in one commit (`7d341f8` on the branch): `sample_design.json` +
`L1.dxf`/`L2.dxf` + both `.openings.json` sidecars → `fixtures/chief-export/`
(never `examples/` — D51); the 16 verified-workflow screenshots →
`docs/evidence/chief-export/`. **One deviation from the ruling's own
table, stated rather than silently done:** the zip's own
`design-schema_v5.json` was **not** unpacked — hash-verified byte-identical
to the already-vendored `floorplanner/design/design-schema.v5.json`, so
copying it would be a second copy of one file the ruling's own §2 argues
against in spirit (three tables that are synced become three tables that
disagree again — the same applies to two schema files). A judgement call,
flagged here rather than made silently.

**§6 — the README split.** §1 of the handoff `README.md` (an integration
task list — "add a menu item", "wire it to config", "show a completion
summary") is spent: everything it asked for is built on this branch, so it
was not carried into the tree at all, not even to `superseded/` (it was
never authoritative documentation, only a spec). §§2-5 (design rationale,
the verified Chief Architect import workflow, limitations) moved into a
new **"Export to Chief Architect (DXF)"** section in the root `README.md`,
placed beside "Import a plan from a PNG" (the closest existing analog — a
workflow guide with numbered steps and screenshots). **The workflow section
is transcribed, not summarised** — every one of the 16 screenshots is
embedded, and the settings the ruling named as traps ("Show Import
Assistant ON", "Create CAD Blocks OFF", "Polylines OFF", "Boxes OFF") are
carried verbatim, in bold, exactly as the original states them.

**§7 — the golden-file receipt.** `L1.dxf`/`L2.dxf`/both sidecars
**regenerated** against the current, `STD_T`-wired `convert()` — the zip's
originally-shipped pair was generated by the exporter *before* the
thickness rewiring §2-§3 already measured as done. **The diff, measured
entity-by-entity (not eyeballed off a 591-line text diff, which is mostly
line-shift noise from every downstream coordinate on an edited wall):**

| wall type | old (shipped) | new (`STD_T`) | moved? |
|---|---:|---:|---|
| exterior | 6.5″ | 6.0″ | **yes** — every `FP-WALLS` face on an exterior wall, plus the door/window gap lines and `FP-NOTES` tag offsets riding those faces |
| railing | 3.0″ | 2.0″ | **yes** — `FP-RAILS` faces |
| interior | 4.5″ | 4.5″ | no — pixel-identical |
| partition | 3.5″ | 3.5″ | no — pixel-identical |

Both `.openings.json` sidecars are **byte-identical** to the original
hand-off — station spans run along the wall centreline and never depended
on thickness. The sample carries no `fence`/`hedge`/`retaining` walls, so
the other three `STD_T` disagreements `0038-ruling.md` §2 measured are not
exercised by this fixture (noted, not fixed — no fixture change was asked
for). Full accounting also lives in `tests/test_fp2dxf.py`'s module
docstring and `fixtures/README.md`'s new row, so it does not need
re-deriving a third time.

`tests/test_fp2dxf.py`, 7 tests: `convert(sample_design.json)` reproduces
the committed `L1.dxf`/`L2.dxf` and both sidecars byte-for-byte (4 tests);
a positive control that the sample converts clean with **zero** warnings —
without it the byte-for-byte tests would pass just as happily against a
golden file that silently contained nothing; two tests driving the menu
wiring itself through a real `MainWindow` (a successful export writes a
real DXF containing `FP-WALLS` for a scene-built room; a write failure is
caught and reported without a modal or an uncaught exception).

**§8 — the menu item + completion dialog.** File ▸ Export ▸ Chief
Architect (DXF)… — `planio.py`'s `export_dxf` (the handler;
`QFileDialog.getExistingDirectory`, since `convert()` writes one
`<level>.dxf` + `<level>.openings.json` pair per storey level, so a
single-file dialog does not fit) and `export_dxf_path(outdir,
interactive=True)` (the non-interactive convention already used by
`export_legacy_v4_path`/`_import_rooms` — a scripted/test caller passes
`interactive=False` and reads the return value or the status line).
Mirrors `export_legacy_v4`/`export_legacy_v4_path`'s existing shape in
`planio.py`, wired in `mainwindow.py` beside "Export legacy v4…".
Serializes the CURRENT scene through `design_document()` — the same
payload Save writes, not a fixture. `convert()`'s `ValueError`/`OSError`
is caught exactly like every other IO failure in this mixin (SS4's "ONE"
finding: a raw exception inside a Qt slot presents as a segfault with no
traceback, `SESSION_SNAPSHOT` §5). On success the dialog reports files
written, skipped site levels, and every warning — `_report()`, the same
helper the Open-conversion report already uses, so this is SS4's "TWO"
finding closed: `convert()` already collected the summary/warnings on
`ConvertResult`, this only surfaces them instead of printing them to a
console a GUI does not have.

**Lint note re-verified, not assumed.** `0038-ruling.md` §4's dead `stem`
variable: grepped `floorplanner/export/fp2dxf.py` directly — gone. A prior
session's claim, checked rather than trusted.

## 2. GATE

Full mode, on the branch, after all four commits: `collected=761
ruff=clean vacuous=0 end_assign=0 snapshot=current`; OFF/ON/DEEP all `754
passed, 7 deselected`; **GREEN**. `python -m ruff check .` clean over the
whole tree, independently confirmed.

## 3. THE SNAPSHOT MECHANISM, ONE THING WORTH RECORDING

`tools/gate.py`'s snapshot-freshness check is branch-relative (marker must
be `HEAD` or `HEAD~1` of whatever is checked out) — and `main`'s own
`54b4a88` had already spent its one commit of slack against the inherited
marker (`6a22aee`) *before* this branch's first commit landed. So the
branch's very first commit already needed a re-cut, regardless of how the
branch's own work was bundled into commits. Handled as its own small commit
(`d670574`) on the branch, pointing the marker at the branch's own tip and
saying so explicitly in the `main` row's text (`origin/main` itself did not
move — it is still `54b4a88` as of this report) rather than silently
implying `main` had advanced. Flagging this because it is not obvious from
reading the ruling alone: a long-lived feature branch under this gate will
need to do the same thing again if it accumulates more than one commit
past its fork point.

## 4. TIER AND WHAT IS NOT CLAIMED

**AMBER, per `0038-ruling.md` §8.** Patrick's acceptance test — export the
two-storey plan, import one level into Chief Architect X17 following the
README's workflow section, confirm walls/doors/windows arrive as their own
native kinds — needs real Chief Architect desktop software this agent
cannot run. **Not claimed as done.** Everything on the FloorPlanner side
(exporter, fixtures, docs, menu, dialog, golden test, gate) is built and
verified on this side only. PR #33 is not merged by this session; Patrick's
manual check is the merge condition, per `docs/ROADMAP.md` §1's AMBER row.

## 5. `FIXTURES/INCOMING/`, per the mailbox protocol's own standing ask

**One file: `crossfloor-snap-2026-08-17.json`, no `.txt` companion note.**
Unrelated to this session's own task (it is the cross-floor-snapping thread
— `0035`/`0036`/`0037-ruling.md` — not this `0038-ruling.md` DXF work); not
touched here. **It is itself a finding, stated per the protocol rather than
left silent:** `git log --follow` on it shows its last touch was
`2c9c075` ("Cross-floor investigation"), cited in `0038-report.md` and
still flagged in `SESSION_SNAPSHOT.md` as "one handoff old, not yet two"
at that point. Eleven numbered handoffs have landed since (`0039` through
this report, `0050`) without it being triaged further — well past the
two-handoff threshold the mailbox protocol names as itself evidence
("evidence arriving faster than it is being read"). Not this session's
thread to resolve; naming it so it is not silently carried an eleventh
time.

## 6. TIER

**AMBER.** [`0044-ruling.md`](0044-ruling.md) §3's last item is now built
and PR'd, not merged.
