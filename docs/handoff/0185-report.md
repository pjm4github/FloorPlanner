# 0185 — report: R4g closed and merged on his word; where the roofline plan stands for the next session

**Patrick, 2026‑09‑13, on [`0184`](0184-report.md):** *"that looks
absolutely perfect!"* — the short-ridge fixture's 3D view and plan. On
that word PR #60 (`roofs-r4g-full-footprint`) was landed on `main` at
`c52396b`, fast-forward, and the branch deleted local and remote in the
merge step; `roofs-r4f-multi-ridge` (PR #59, closed in favour of #60)
deleted with it. Only `main` remains on the remote. Gate GREEN at rest,
1287 tests.

## 1. What R4g closed, in one place

* [`0176`](0176-ruling.md)/[`0177`](0177-ruling.md): candidacy is the
  roof's own footprint; reachability is anchored at single-coverage
  ground; a point contact connects nothing.
* [`0179`](0179-ruling.md): prune-and-re-envelope, a fixed point.
* [`0181`](0181-ruling.md): the under-pass network — a roof connects to
  its anchor by passing UNDER a real roof, never by floating ABOVE one.
* [`0182`](0182-report.md): built, plus the two facts the fixtures
  forced — a swallowed end that has crossed its host's ridge is the
  phantom (pruned one at a time, shorter overshoot first), and a joining
  end's extension never comes back up. Pruning per lost piece. 0% blank
  on the three-ridge fixture; D85's corner the L's one legal blank.
* [`0183`](0183-report.md): seams read off the final pieces (every
  equal-height shared edge between two roofs).
* [`0184`](0184-report.md): a ridge end stopping short of its host's
  ridge, standing above it, is an open gable end; a crossed end never
  opens. His second fixture promoted.

The partition invariant is asserted globally through
`compute_roof_clips(..., diag=)`; `docs/evidence/threeridge-drawn-junction-0182.png`
is the drawn junction beside his v2 reference.

## 2. Open, for his ruling — nothing owed by Code

* **Crossed arms that survive stay joined** ([`0184`](0184-report.md)
  §4): rf2's south end at the pinch is 4" above rf3 and is drawn with the
  wrap-around extension his v2 reference has, not a 4" fascia. One line
  to change if he wants it open; not changed without his word.
* **The rule reads the ridge END point only** — a joiner below its host
  at the ridge but above it at an eave corner stays joined; no fixture.
* **Named limits carried from 0182 §5:** the fixture clip 0.11 s →
  0.36 s; four sub-inch slivers at (756, 553); a join end's cut-off root
  corner touching hidden ground would certify.

## 3. The queue, unchanged

**R5 dormers** — RED behind [`0164`](0164-ruling.md) §3, its own ruling
when he wants it. **Named, not ordered:** valley/hip lines as drawn
plan lines, a roof-plan export sheet, yard items. **Carried, undated:**
room-label rounding ([`0131`](0131-ruling.md) §2), delta-snap sites, the
D61 family, D83/D84 (macro-recorder gaps, held).

**Carried:** unchanged from [`0184`](0184-report.md).
