# The handoff mailbox — protocol

**Two agents work on this repository: one writes code, one rules on it. This
directory is where they hand things to each other.** The rule that makes it
worth having is short:

> **Chat is not the record. A report is complete when it is on disk; a ruling is
> authoritative only from its file.**

That is not a new rule. It is the Working agreement's *"a checkpoint is not
complete until its handoff spec is committed"*, given a place to live.

---

## The protocol

1. **Code writes `NNNN-report.md` and commits it.** A read-back, a census, a
   measurement, a question — anything that needs an answer before work can
   continue. Reporting it in a terminal is not reporting it.
2. **The reviewer writes `NNNN-ruling.md` and commits it.** Decisions, with
   their reasons. A ruling that exists only in a conversation cannot be quoted
   later, cannot be found by the next session, and cannot be disagreed with on
   the record.
3. **The numbering is sequential and shared.** A report and its ruling take the
   same number. Next free number = highest here plus one, archive included.
4. **A closed pair moves to [`archive/`](archive/) when its task ticks.** The
   mailbox shows what is live; the archive keeps what was decided.

## THE CHANNEL CONTRACT — Patrick's standing change, 2026‑08‑14

**The diagram: [`channel-commands.svg`](channel-commands.svg)** — the five
phrases (four to an agent, one to your own terminal), the AMBER stop, and both
recovery paths: Code's `checkpoint` before a `/clear` and `resume from NNNN —
and MMMM is up`, Cowork's context-free `where do things stand?` reboot (it
holds nothing that needs recovering, since every decision was written to disk
in the turn it was made).

**This directory is the channel, not a record kept alongside one.** Patrick's
Cowork session reads this repository directly; he no longer relays reports by
hand. So a report written *for a terminal to carry to him* is written for a
reader who no longer exists, and the protocol above is now stricter than its
first sentence suggested — this is what it means in full:

* **The terminal gets one short paragraph**: what was done, what is needed, and
  the file number. **Nothing else.** The report, census, read-back or receipt
  itself goes to `docs/handoff/NNNN-<kind>.md`, committed, in full.
* **Code does not write `-ruling.md` files, ever.** The reviewer writes
  `docs/handoff/NNNN-ruling.md` directly, on disk. Code's job is to **read it,
  act on it, and cite it** — not to transcribe it into a file of its own.
  "Record this ruling" means *cite the file the reviewer wrote*, not *author a
  copy of it*.
* **Never edit a file the other side wrote. Never expect the other side to edit
  one of yours.** Each side only *creates new numbered files*. A correction is
  the **next number**, in the open — not a silent edit to an earlier one.

**THE SUFFIX SPLIT (`-report.md` vs. `-ruling.md`) IS NOT A NAMING CONVENTION —
IT IS THE MECHANISM THAT MAKES THE COLLISION IMPOSSIBLE.** Two writers touching
the same file, even at different times, is exactly the shape that has already
cost sessions here: the append-only `side-tasks.md` conflict this project
measured and fixed by moving to per-file logs, and the case where a reviewer's
correction very nearly got written into the wrong party's words on the strength
of a premise the implementer had already disproved. **If each side owns its
suffix absolutely, there is no file either side is ever tempted to open in
place of the other's** — the split removes the failure mode rather than asking
either party to remember not to trigger it.

**Auto-commit is permitted for GREEN-tier work** — Code commits, pushes and
merges without asking, per the autonomy policy. **The pre-commit hook (a fresh
green gate, newer than every tracked file) is the only bar**, unchanged by this
rule. **AMBER still stops for Patrick's manual check before merge**; nothing
merges on a red gate or a failed check. This rule changes *reporting*, not
*authority*.

## How the rest of the record refers to a pair

**A progress entry CITES its handoff — one line, `handoff: 0042` — and does not
restate it.** Ruled 2026-08-06, and the reasoning is worth keeping: the progress
log is *curated* (what happened, why, what proved it) and a handoff file is *raw*
(the exchange itself). Different genres, different readers. Collapsing them would
make the log inherit the exchange's verbosity, and the log is already 4,351
lines. Same relationship the log already has with `docs/evidence/`: cite the
artifact, do not inline it.

## Every handoff lists `fixtures/incoming/`, with the age of each file

**Ruled 2026‑08‑09.** The intake directory
([`../../fixtures/incoming/README.md`](../../fixtures/incoming/README.md)) is
where Patrick drops plans that break or look wrong, uncharacterised and
unreferenced by any test. It is invisible to the gate **by design**, which means
nothing else will ever mention it.

So a report **names every file in it and how old each one is**. And **a file
that has sat there across two handoffs without triage is itself a finding** —
evidence arriving faster than it is being read — and it is stated out loud
rather than left to accumulate. The three exits (promote with a fail-first test,
delete as a duplicate naming its cover, delete as no-defect-found naming what
was checked) are in that README.

## What belongs here, and what does not

| | |
|---|---|
| **here** | read-backs, pre-work censuses, rulings, findings that need a decision, disagreements and how they resolved |
| **`../progress/`** | what was done, in the order it was done, with its gate |
| **`../defects/`** | a fault, gap, limit or task that outlives the exchange |
| **`../evidence/`** | the measurement itself, and the probe that produced it |

A report that turns out to describe a defect gets a record in `../defects/`; the
report is not the register, and the register is not a conversation.

## Two conventions that keep the pair readable

**Quote the ruling, do not summarise it.** A ruling file carries the reviewer's
words. A summary of a decision is a second version of it, and this project has
measured what second versions do.

**A report states what it measured and what it could not.** The instrument's
boundary belongs in the report, because the ruling depends on it — several
rulings in `0001` turned on exactly that.

---

**ONE LINE PER PAIR, per [`0028-ruling.md`](0028-ruling.md) §5** — the number
and a single clause naming the subject. The reasoning is in the file linked;
this table is not a second copy of it.

| pair | subject |
|---|---|
| [`0001`](0001-report.md) · [`0001-ruling.md`](0001-ruling.md) | The docs refactor read-back and its rulings |
| [`0002-report.md`](0002-report.md) | Repo state 2026‑08‑09 — the vertex-accumulation programme |
| [`0003-report.md`](0003-report.md) | D61's three owed items measured; D62 filed |
| [`0004-report.md`](0004-report.md) | The leave path does not weld; 2a's fix partly evaporates on save |
| [`0005-report.md`](0005-report.md) · [`0005-ruling.md`](0005-ruling.md) | Reboot state 2026‑08‑10 — PR #19 unreviewed |
| [`0006-readback-outline-invariants.md`](0006-readback-outline-invariants.md) | Read-back — the two OUTLINE invariants, which corpus files fail each |
| [`0007-readback-phase-6.md`](0007-readback-phase-6.md) | Read-back — Phase 6 does not retire `snapshot()` |
| [`0008-readback-phase-6-deep.md`](0008-readback-phase-6-deep.md) | Read-back — Phase 6 is a CUTOVER, not a retirement; 2b survives |
| [`0009-readback-p6d-cutover.md`](0009-readback-p6d-cutover.md) | Read-back — P6.d's three questions; D67 does not block the cutover |
| [`0010-census-furnishings.md`](0010-census-furnishings.md) · [`0010-ruling.md`](0010-ruling.md) | Census — furnishings; 28 of 95 fall back to a box; D70 filed |
| [`0011-census-wall-types-and-railings.md`](0011-census-wall-types-and-railings.md) | Census — wall types/railings mostly already built; D73 filed |
| [`0012-readback-prism-outlines.md`](0012-readback-prism-outlines.md) · [`0012-ruling.md`](0012-ruling.md) | Read-back — prism outlines; build prism, re-measure, then decide |
| [`0013-report-prism-receipt.md`](0013-report-prism-receipt.md) · [`0013-ruling.md`](0013-ruling.md) | Prism's receipt — 28→1 corrected to 27-of-28; AMBER, backed out and re-applied |
| [`0014-report-furniture-regions.md`](0014-report-furniture-regions.md) · [`0014-ruling.md`](0014-ruling.md) | Report — furniture regions; 17-of-18 outlines are a plain rectangle |
| [`0015-ruling.md`](0015-ruling.md) | Ruling only — `seat`/`bed`/`basin`/`enclosure` retired, `vehicle` not |
| [`0016-ruling.md`](0016-ruling.md) | Ruling only — "chunky boat trailer" withdrawn; `enclosure` conflates vessel and room |
| [`0017-report.md`](0017-report.md) | Report — the SS5 measurement; all three items read WELL |
| [`0018-ruling.md`](0018-ruling.md) | Ruling — 0017's control pointed the wrong way; `enclosure` splits into `vessel`/`enclosure` |
| [`0019-ruling.md`](0019-ruling.md) | Ruling — a generated status board replaces the frozen migration table |
| [`0020-ruling.md`](0020-ruling.md) | Ruling — the coordination protocol, on disk for the first time |
| [`0021-report.md`](0021-report.md) | Report — the vessel/enclosure split, built; D75 filed |
| [`0022-ruling.md`](0022-ruling.md) | Ruling — the control accepted; row 1 not discharged by the mesh numbers alone |
| [`0023-ruling.md`](0023-ruling.md) | Ruling, GREEN — session continuity; the checkpoint/resume protocol recorded |
| [`0024-report.md`](0024-report.md) | Report — 0022's remedies built; D76 and D77 filed |
| [`0025-ruling.md`](0025-ruling.md) | Ruling — the check passes, verbatim; push, open the PR, merge on green CI |
| [`0026-report.md`](0026-report.md) | Report — PR #31 pushed, CI red on a structural finding; D78 filed |
| [`0027-ruling.md`](0027-ruling.md) | Ruling — a self-correction, then D78's remedy (b), adopted and receipted |
| [`0028-ruling.md`](0028-ruling.md) | Ruling, GREEN — trim this file and `SESSION_SNAPSHOT.md` to their stated job |
| [`0029-ruling.md`](0029-ruling.md) | Ruling — the extrudability predicate goes first, GREEN; then the redraws, AMBER |
| [`0030-ruling.md`](0030-ruling.md) | Ruling — the glance test fails today (baseline render); a mark in the render contradicts D76, reconciliation ordered |
| [`0031-ruling.md`](0031-ruling.md) | Ruling, GREEN — the glance fixture placed (`shower-glance-check.json`), a Cowork status view recorded, four untested check fixtures named as a deferred gap |
| [`0032-report.md`](0032-report.md) | Report — predicate built (with a correction to predicate 2), census run (6 more fragmented items found), D76 reconciled (stands, unamended); the redraw brief changes to `beside`-shaped marks, not regions |
| [`0033-report.md`](0033-report.md) | Report — the three redraws built, AMBER, one honest limit. **Cherry-picked onto `main` at `0040`'s §4 remedial** (was branch-only on `shower-identity-redraws`/PR #32 until then) |
| [`0034-ruling.md`](0034-ruling.md) | Ruling — the check is ready, the camera is the open question, `0030` §4 withdrawn. **Cherry-picked onto `main` at `0040`'s §4 remedial** |
| [`0035-ruling.md`](0035-ruling.md) | Ruling — Patrick's own report: cross-floor snapping (a census must test BOTH a missing filter and mis-tagged floor data, since the obvious query paths already filter correctly) and a per-floor totals feature, blocked on open D55 (totals double-count overlaps) — GREEN measurement can start now, neither displaces `0033`'s check or grid snap |
| [`0036-report.md`](0036-report.md) | Report — `0034`'s four action items, built. **Number collision** with this file's own `0036-ruling.md`; both legitimately committed on their own branches, neither renamed; see `0038`. **Cherry-picked onto `main` at `0040`'s §4 remedial** |
| [`0036-ruling.md`](0036-ruling.md) | **Number collision** — this file and `shower-identity-redraws`'s `0036-report.md` share a number; both legitimately committed on their own branches, neither renamed; see `0038`. Ruling — snap vs. bleed-through are different faults with no shared code; the discriminator is whether the saved document changes across the gesture, and it must run before `0035`'s census |
| [`0037-ruling.md`](0037-ruling.md) | Ruling — light gray IS the ghost paint, so `show_others` was true; names a suspect (the load path setting only `active`) and revises the census to Qt-reachability paths, not `.floor`-filtered ones |
| [`0038-report.md`](0038-report.md) | Report — `0037` §2's suspect does not hold: `apply_design_to_scene` already calls `_sync_floor_state()` on load (`bridge.py:1265`, since 2026‑07‑26), measured correct on Patrick's own submitted plan both with and without `show_others` pre-set. Reopens `0036`'s document-diff discriminator, still unanswered |
| [`0038-ruling.md`](0038-ruling.md) | **Number collision** — this file and this session's own `0038-report.md` (unrelated, about the floor investigation) share a number; neither renamed; see `0039`. Ruling — `fp2dxf` (a v5→DXF exporter built outside the repo) accepted in principle; its thickness table is D73/D74's disease again (one column carrying both a real quantity and a Chief-type mapping); three library-hygiene faults named; AMBER, ordered behind `0033`'s check and the floor work |
| [`0039-report.md`](0039-report.md) | Report — blocked: `fp2dxf.py`, its README, sample input/output and screenshots are not anywhere in this repository (checked exhaustively). Nothing in `0038` can start until it is dropped somewhere readable |
| [`0040-ruling.md`](0040-ruling.md) | Ruling — the package landed (`0038-fp2dxf-handoff.zip`, verified CRC); `0037` §2's suspect withdrawn (the v5 load path already recomputes floor visibility, since 2026‑07‑26); the "report and ruling share a number" convention retired — a ruling names the report it answers instead; the mailbox is a record and lives on `main` only, never a feature branch; `0033`–`0036-report.md` cherry-pick onto `main` still owed and gates the next number |
| [`0041-ruling.md`](0041-ruling.md) | Ruling — Code hit its context limit before a checkpoint; recovery recorded from the artifact alone: gate GREEN, `fp2dxf.py`/`__init__.py` on disk with the by-path decision written into the docstring, zip still packed. Recovery owed: re-run the gate, commit as a recovery (not a checkpoint), a short report |
| [`0042-ruling.md`](0042-ruling.md) | Ruling — answering Patrick's "is CI overkill" question: no, `Docs-Snapshot` is the only check ever at fault and it is miscategorised, not excessive — a git-topology check wearing a CI job's clothes; moved out of the `pull_request` lane, kept on push-to-`main` and the local gate, which already prevents the fault before a commit exists |
| [`0043-report.md`](0043-report.md) | Report — recovery closed: gate re-run found one new `ruff` finding (`B905`, fixed), committed at `5d61f1f`; measured against `0038`'s owed list, all three library-hygiene fixes and the `STD_T` thickness rewiring are already done, not merely started; zip still packed, no sample/README/golden test yet. **Numbered `0043` not `0042`**: `0042-ruling.md` landed mid-session and took the number first. `0040`'s cherry-pick remains owed and still gates the next number |
| [`0043-ruling.md`](0043-ruling.md) | **Number collision** — this file and Code's own `0043-report.md` share a number, a third time; see `0044`. Ruling — answering Patrick's "is the gate too rigorous" question: the friction is that only full-mode gate output unlocks a commit, not the gate's rigour itself; moves the bar from every commit (`--quick`) to every push (full mode), unchanged strength at what reaches `main`/origin/CI; also owes a flap receipt — run the gate twice on one unchanged tree before trusting a "flapping" claim |
| [`0044-ruling.md`](0044-ruling.md) | Ruling — `0043`'s collision was a genuine race (two writers, no lock), not the branch split; retires nothing further, but adds a protocol refinement: name the suffix when flagging a shared number (`"0043-ruling is up"`, not `"0043 is up"`). Names the order of everything owed: push `main`, cherry-pick `0033`-`0036-report.md` (this commit), the flap receipt, `0042`'s CI-lane move, `0043`'s hook split, then the DXF integration last, on a fresh context |
| [`0045-ruling.md`](0045-ruling.md) | Ruling — Patrick's shower check was run against `main`, which never had the redraws; they and the after-render exist only on `shower-identity-redraws`. Third instance of one root cause (work needed for a check living on an unnamed branch); adds "name the BRANCH" as the third check-request clause alongside "name the PLAN" and "name the CAMERA". Tier NONE — corrects how the check is run, changes nothing; `0044` §3's order stands |
| [`0046-report.md`](0046-report.md) | Report — `0044` §3 items 2 (mailbox cherry-pick) and 3 (flap receipt: gate run twice on one unchanged tree, identical both times, no flap) done, doc-only/measurement-only. Items 1 (push), 4 (CI-lane move) and 5 (hook split) held — push and infrastructure edits, CLAUDE.md's own etiquette says push only when explicitly asked, held to the same standard here despite `0044`'s GREEN tier. Item 6 (DXF) untouched per `0044`'s own instruction to start it fresh |
| [`0047-ruling.md`](0047-ruling.md) | Ruling — all three held items authorised: push needed no asking (the autonomy policy already covers it, verbatim), the CI-lane move authorised as ruled at `0042`, the hook split authorised but `0043` §7's one control is not enough — needs FOUR (no-result/quick-GREEN/full-GREEN/any-RED, each at both commit and push), in its own commit, revertible alone. The line: autonomy covers work the gate judges, not changes to the judge |
| [`0048-report.md`](0048-report.md) | Report — item 4 done: `Docs-Snapshot` skipped in the `pull_request` lane via a new `_snapshot_check()` wrapper at the two call sites (`main()`/`_docs()`), not inside `_snapshot_head()` itself, so every existing `HEAD^2`/merge-ref test keeps exercising the real logic unchanged. Two new controls (skip-when-stale positive, skip-scoped-to-PR negative), 13/13 green, end-to-end CLI check under a simulated PR env. One pre-existing, unrelated `Docs-Verdict: RED` (30 unresolved doc refs) flagged, not fixed |
| [`0049-report.md`](0049-report.md) | Report — item 5 done: the commit hook now matches `git push` too, requiring `mode == "full"` in `.gate-result.json` (a new field `--quick` now also writes); `git commit` still accepts either mode. `tests/test_verify_gate_hook.py`, 18 tests against an isolated fixture repo, driving all 8 cells of `0047`'s table plus distinct-message and freshness controls — caught a real `"pushs"` pluralisation bug before it shipped. 31/31 green (with `0048`'s 13), full gate GREEN |
| [`0050-ruling.md`](0050-ruling.md) | Ruling — Patrick's check on `shower-identity-redraws` passes (the three enclosures read as different things); the branch is 9 behind `main` and must not merge as checked — bring `main` in, full gate on the combined tree, re-run the extrudability census and report the three redrawn symbols, retake the after-shot only if the render changed, then PR/green CI/merge. AMBER condition already met, subject to stating which camera Patrick used and the re-gate |
| [`0050-report.md`](0050-report.md) | **Number collision** — this file (the worktree agent's own, landed autonomously) and `0050-ruling.md` share a number; the collision `0051-ruling.md` §2 predicted before it happened. Neither renamed, per `0040` §4's judgement. Report — `0044` §3's last item (the `fp2dxf` DXF integration) built end to end on branch `fp2dxf-integration`: zip unpacked/deleted, golden DXF pair regenerated against `STD_T` (diff stated: only `exterior`/`railing` moved), README split (workflow section transcribed verbatim), the File ▸ Export ▸ Chief Architect (DXF)… menu action + completion dialog wired, 7-test golden-file receipt, gate GREEN, ruff clean. [PR #33](https://github.com/pjm4github/FloorPlanner/pull/33) open. AMBER — Patrick's Chief Architect manual check is the merge condition and is out of Code's reach |
| [`0051-ruling.md`](0051-ruling.md) | Ruling — the DXF worktree agent is the right call, but structurally guaranteed to collide on a handoff number (it forked before `0050` existed) and to write its report on its own branch, not `main`. New rule: a worktree agent does not take a number or land a mailbox file; the parent reserves the number and lands the report on `main` itself. Retrofit for the run in flight: let it finish, then the parent renumbers and lands it |
| [`0052-ruling.md`](0052-ruling.md) | Ruling — the export guide lands at `docs/guides/chief-architect-export.md`, a new category (first user-facing doc in the repo); §1 of the zip's README is deleted as a spent handoff spec, §§2/3/5 become the guide, screenshots stay at `docs/evidence/chief-export/` with paths rewritten to `../evidence/chief-export/...`, and the guide must carry the current `STD_T`-derived thickness numbers, not the zip's stale ones. GREEN, separable from the AMBER menu item |
| [`0053-ruling.md`](0053-ruling.md) | Ruling — reads the git widget for Patrick: the DXF agent finished (`fb054e3`, took `0050` exactly as `0051` predicted), the worktree/branch are spent and owed deletion. Order: `git pull` main, merge PR #32 (ready, only the census statement was owed), leave PR #33 for Patrick's Chief check (on the regenerated DXF, not the original sample), delete five spent branches. Confirms the numbering-fix rule is now owed, not optional, since the collision happened while the agent was already in flight |
| [`0054-report.md`](0054-report.md) | Report — Patrick's Chief Architect check PASSED, quoted verbatim: the regenerated `L1.dxf`/`L2.dxf` exported from `fp2dxf-integration` and imported into Chief Architect X17, closing `0038-ruling.md` §8's merge condition. Branch brought current with `main` (8 behind, one real conflict in `fixtures/README.md`), re-gated GREEN. One review-checklist gap flagged for the record: the menu item only exists on the unmerged branch, and its label is a flat File-menu entry, not a nested submenu |
| [`0055-ruling.md`](0055-ruling.md) | Ruling — grid snap will not fix the off-axis walls Chief flagged on Patrick's own export; the exporter itself is clean (0 off-axis in the committed sample); Patrick's document shows two populations, `L1`'s large angles likely real architecture, `L2`'s 75 sub-2° deviations measured as drift from move/join/weld/coalesce relocating a vertex, which snap (cursor input only) cannot touch. Orders an orthogonality REPORT first (GREEN, cheap), a REPAIR is RED and unauthorised pending its own tolerance ruling, grid snap unchanged but its read-back owes one more clause |
| [`0056-report.md`](0056-report.md) | Report — item B built, item B ONLY per instruction: `wall_orthogonality()`/`orthogonality_bands()` in `validate.py`, wired into both surfaces `0055` asked for (`tools/validate_design.py`'s corpus census, Edit ▸ "Wall orthogonality report…"). Cross-checked against `0055`'s own corpus numbers (6/2 off-axis walls on `planc1.v5.json`/`symmetricP1.json`) rather than assumed. 17 new tests, both controls, gate GREEN. Item C (the repair) and grid snap (item A) untouched, as instructed |
| [`0057-ruling.md`](0057-ruling.md) | Ruling — the instrument is accepted (four things done right unprompted: cross-checked numbers, named the DXF-line/wall-count mismatch, the positive-control-for-zero, ASCII labels) but the census itself — B's whole justification — was never run. Orders it: every file in `examples/`+`fixtures/`, per-plan band table, the corpus total within 1° of orthogonal. Also owes one guide line (check the report before exporting) and notes the export-warning question (not ruled) and an Edit-menu placement gap (not worth its own fix) |
| [`0058-report.md`](0058-report.md) | Report — the census run: 948 walls across 16 v5 plans (8 skipped, each named why), 63 within 1° of orthogonal without being on it — `0055`'s own input to item C's tolerance argument. Two more of `0055`'s own numbers independently reproduced (both wiscaway files, exact match). One outlier named, not triaged (a live cross-floor-snapping intake file, different thread). Guide line added to `README.md` where the guide actually lives (not `docs/guides/`, which `0052` specified but `0054` found superseded). Export-warning question left open, as scoped |
| [`0059-ruling.md`](0059-ruling.md) | Ruling — the census stands, but its printed table cannot produce its own 63 headline: the bottom band merges "exactly on axis" with "off by up to 0.01°", so 51 of the 63 are visible and 12 are not. Orders the bucket split (`0.01-0.1`, `0 < dev < 0.01`, `on axis` as its own row), then a re-run. `0052`'s guide-location ruling withdrawn, superseded by where the branch had already put it. Names the crossfloor-snap outlier as a question for `0037`'s thread, not this one, with a cheap separating measurement suggested (its off-axis rate vs `wiscaway2026-08-09R`'s) |
| [`0060-report.md`](0060-report.md) | Report — the band split built and re-run: the table now sums to 63 exactly. Found and fixed a latent column-width bug in both printers, exposed by the new longer label. Ran `0059`'s suggested comparison: it does not separate the two hypotheses, and where it points, it points away from the bug — `wiscaway2026-08-09R` (no reported cross-floor symptom) has MORE walls over 5° (53) than the crossfloor plan (36), correcting `0058`'s own "highest in the corpus" framing. Also ran `0037` §3's reachability census: every mouse/macro hit-test and selection path shares one root (`items.py`'s `hit_candidates()`) and none filter by `.floor`, versus 100% of `walls.py`'s geometry hot paths, which do — currently masked by `apply_floor_visibility` running at load, not a reproduced bug, no fix built (AMBER territory per `0037` §6). GREEN, measurement only |
| [`0061-ruling.md`](0061-ruling.md) | Ruling — withdraws both of `0059` §4's own claims (crossfloor is not the worst on the flagged band; the rates are not the same) and accepts `0060`'s correction verbatim. The census found the fault: `view.py:244` `_align_to_wall` scans `sc.items()`, not `items(pos)`, so it is not masked by `apply_floor_visibility` the way the other reachability sites are — a real, live cross-floor snap on the wall-drawing path, matching Patrick's report clause for clause. Orders a fail-first test first (GREEN), then the fix (AMBER, Patrick's manual check as the merge condition), then names `fixtures/incoming/crossfloor-snap-2026-08-17.json`'s exit as owed |
| [`0062-report.md`](0062-report.md) | Report — the fail-first test built (RED before the fix: `500.0` where `505.0` was drawn), the fix applied (`_align_to_wall` and `wall_endpoint_open` both gained the `.floor` filter every other geometry hot path already had), full regression pass green, gate GREEN. The four other unfiltered sites `0061` §4 named are scoped out with a stated reason (they use `items(pos)`/`items(area)`, which `0061`'s own §2 found masked, unlike the bare `sc.items()` that was the live bug) and left as named follow-on hardening. `fixtures/incoming/`'s exit taken: the defect is covered by the synthetic test, the file itself promoted to `fixtures/` (kept as independent corpus evidence, not deleted). AMBER — branch `cross-floor-align-fix`, PR open, stopped for Patrick's manual check |
| [`0063-ruling.md`](0063-ruling.md) | Ruling — the fix and `wall_endpoint_open`'s new `floor=` param are both accepted, the latter praised as more than asked and reasoned from the mechanism; `0062` §3's refusal to fix the four masked sites without a receipt is sustained against `0061`'s own contradiction. Three findings: the fail-first receipt is a negative assertion with no positive-control pairing (D43/`0022` §2 — owed, two lines); the `incoming/` exit's "census is unaffected" reason is vacuous by tautology (the census already swept `incoming/` before the move) though the outcome stands on its other leg; and the mailbox landed on the feature branch again (fifth instance, first time the RULING did too) — ordered onto `main` now, doc-only, before any merge decision. Also flags `wall_endpoint_open(floor=None)`'s default as the bug's own shape, named not ordered |
| [`0064-report.md`](0064-report.md) | Report — all three of `0063`'s GREEN items done: the mailbox pair landed on `main` doc-only, the positive control added to the fail-first test and pushed to PR #34 (still AMBER, still open, still stopped for Patrick's manual check), `fixtures/incoming/README.md`'s fourth exit added. Also reports two features outside the handoff queue entirely, Patrick asking directly in-session: the status bar shows the selected wall's id + vertex ids, then extended with each vertex's (x, y) in decimal feet, the wall's length, and its heading when not axis-aligned — both additive, gate GREEN, pushed straight to `main` |
| [`0065-ruling.md`](0065-ruling.md) | Ruling — `0063`'s three items independently re-verified (own census run from raw JSON, exact match to `0060`'s table). Two defects in the wall label: the angle clause's 1-decimal rounding prints a false exact cardinal on 20 of the corpus's 63 drifted walls; `fmt_ft3`'s significant figures give only 1ft resolution above 100ft. Amends `ROADMAP.md`'s tier table — Patrick's own instruction is authority ranking above a ruling, not a fourth tier — since the table as written would force RED onto work its own principal ordered. Also finds the aborted `cross-floor-align-fix` merge established the branch-snapshot rule correctly (adopted from `ea215ff`), and that `0063`'s own contract for `incoming/`'s exits was missing this case too — third such gap, each found by Code trying to obey it. Item C reserved as `0066`, Patrick's own, not to be pre-empted a fourth time |
| [`0067-report.md`](0067-report.md) | Report — `0065`'s two GREEN items landed on `main` (the tier-table sentence, the cardinal-suppression test extended to all four). The two label defects fixed in one commit, batched per `0065` §7: the angle clause now prints at 4 decimals (never rounds to a false cardinal, receipted at 0.0001° off axis); `fmt_ft3` renamed `fmt_ft2`, fixed 2 decimals (Patrick's own choice, asked directly, over 3 decimals or keeping significant figures). AMBER — [PR #35](https://github.com/pjm4github/FloorPlanner/pull/35) open, batched with PR #34's manual check, both merge conditions restated verbatim so neither drifts |
| [`0068-ruling.md`](0068-ruling.md) | Ruling — corrects itself: `0065` §4 misattributed the significant-figure format to Code when `0064` §4, open in front of it, already said Patrick asked for it. `0067`'s handling credited. Finds the named receipt (`test_the_angle_clause_never_prints_a_cardinal`) is vacuous — four hardcoded 4-decimal literals pass at any fixed precision, including the broken `.1f` — so it never went red; also finds a corpus-measured number (0.0002037°) got laundered into a code comment as if `0065`'s own invented test magnitude (0.0001°) were a census result. Orders a format-free round-trip assertion instead, the comment corrected, `main` merged into the branch before any check, and a "quoted in the report" clause closing the authority amendment's own audit gap. Declines to order a fix for `.4f`'s own remaining floor — that question goes to Patrick, not assumed |
| [`0069-report.md`](0069-report.md) | Report — all four of `0068`'s GREEN items done: the round-trip assertion replaces the vacuous one (checked to actually discriminate — reverted to `.1f` locally, confirmed RED, restored, confirmed GREEN), its magnitude now derived and verified inline rather than borrowed; the laundered comment corrected to the real measured number, sourced; `main` merged into `wall-label-fixes` and re-gated GREEN before any check; `ROADMAP.md`'s authority clause gets its "quoted in the report" requirement. `0068` §4's question (heading vs. deviation) restated for Patrick, not answered. Two new untriaged files flagged in `fixtures/incoming/` per the channel's own rule |
| [`0070-ruling.md`](0070-ruling.md) | Ruling — accepts `0069` §1's differential as real. Reproduces Patrick's own off-grid report (`fixtures/incoming/w7offgrid.fpm` / `w7offsetFloorplan.json`): a fresh interior wall started at a T-junction against an existing wall's body lands off the 6″ grid, one bad number propagated by welding to five vertices, every wall perfectly orthogonal — NOT `0055`'s drift fault (tilted walls near grid); this one is straight walls between grid lines. Reads the mechanism from source (no PyQt6 in this reviewer's environment, stated plainly): the default move/slide path snaps the DISPLACEMENT, not the destination, answering `0055` §4's unanswered clause — snapping covers neither cursor input nor an operation's output, it covers the delta, worse than both options the clause offered. Orders a bisect first (name the exact first bad step), the A/B (weld/coalesce on/off), a fail-first test, one fix, D80, and the intake exit — pre-decided as promotion, since the pair already is a test input |
| [`0071-report.md`](0071-report.md) | Report — `0070`'s full order done. Bisect (replayed `w7offgrid.fpm` one line at a time, both weld/coalesce on and off): identical pattern both ways, first bad step at line 6 — a fresh wall DRAW at a T-junction, not a move; the seed is the draw, not normalisation. Root cause named exactly: `_snap_start` returned `nearest_wall_body_point()`'s raw projection verbatim, `wall_snap` only in the unreached fallback. Fixed at that one site (`_grid_snap_t_junction`, axis-aligned hosts only), receipted (reverted/restored locally, confirmed RED then GREEN), both intake files promoted under exit 1, D80 filed open. AMBER — [PR #36](https://github.com/pjm4github/FloorPlanner/pull/36) open, batched with PR #34/#35's manual checks, all five Patrick questions restated verbatim in one place |
| [`0072-ruling.md`](0072-ruling.md) | Ruling — on Patrick's own request: wire the new `floorplanner/export/fp2pdf.py` into the app, group the File menu's exports, merge CLI switches, give it an options + Save-As dialog. `0071` explicitly not ruled here — gets its own ruling. Measures `fp2pdf.py` repeats four faults `fp2dxf.py` was fixed for five days earlier: a third wall-thickness table disagreeing in 4 of 7 rows, `raise SystemExit` inside `convert()`, `print()` instead of a result object, an unguarded `reportlab` import that would stop the app starting for anyone without it. The CLI-merge ask presupposes an app command line that does not exist — split out, RED, its own ruling. Orders the four hygiene fixes first (GREEN, module not yet reachable from the app), the menu re-parent and PDF dialog after (AMBER), flags four AMBER items now queued behind one Patrick check session |
| [`0073-ruling.md`](0073-ruling.md) | Ruling — amends `0072` §5 on Patrick's own further instruction (quoted in full): `fp2pdf`'s hardcoded settings should be overridden by the tool's own settings where available, new ones added to "the floorplanner settings file" once. Measures there is no such file — settings live in the plan document's own `settings` block, so this is a schema change, not a config edit. Finds a live trap directly in the path: both settings loaders coerce every non-bool value to `float`, silently replacing anything that won't (i.e. every string Patrick wants — title, author, notes) with the default, on every load, unreported — must be fixed (type-aware loader, GREEN) before any string setting is added. Sorts every `fp2pdf.py` constant into three bins (thickness stays read from `STD_T`, genuine settings go to the schema, drafting internals stay constants) and specifies one precedence chain, function written once, for both the GUI and the standalone CLI |
| [`0074-ruling.md`](0074-ruling.md) | Ruling — self-correction: `0073` §1 said no application-level config file exists; `config.py`'s own `app_settings()`/`settings_file()` (a `QSettings` INI) does, thirty lines below where `0073` was reading. Most of Patrick's next ask (a settings file in a default location, standard format) is already built; reading it at load, materialising a default, and Settings ▸ Save writing it are not. Rules a three-rung precedence chain (open document's own `settings` block wins over the INI, which wins over `DEFAULT_SETTINGS`) so a saved plan always reopens the way it was saved. Names, does not execute (no PyQt6 on this reviewer's bridge), a live hazard: `QSettings` INI values are untyped strings, so `bool(value("shuffle"))` on a stored `False` reads back `True` — every editing flag would silently invert. Read-back ordered first; two GREEN items (the round-trip receipt for that hazard, `0073` §2's loader) can proceed without it |
| [`0075-ruling.md`](0075-ruling.md) | Ruling — accepts Patrick's own follow-up: the checkbox, and replace `QSettings` with a plain Python settings store, because a string-typed hazard is not eliminated by picking a stdlib format that has the same one (`configparser`'s `"false"` is exactly `QSettings`'s bug with a different name). Rules JSON over TOML — real types like TOML, but stdlib on the project's own 3.10 CI leg, where `tomllib` needs 3.11+. Names a real migration-ordering trap: if first-run default materialisation runs before INI→JSON migration, a real user's saved `anthropic_api_key` is silently lost, and `catalog.py`'s own bare `except: return ""` would never report it — rules migration-first, materialisation never touches that key, the INI left on disk unread. Specifies the "use as default" checkbox precisely: unchecked by default, never sticky, only the keys the dialog itself shows |
| [`0076-report.md`](0076-report.md) | Report — every GREEN item across `0072`-`0075` built: `coerce_setting()`, one function replacing both settings loaders' duplicated float-coercion loop; the app-settings store rebuilt on plain JSON (`QSettings` dropped entirely), the string/`False`-reads-as-`True` hazard receipted directly; INI→JSON migration with its own ordering-trap receipt (reverted/restored locally, confirmed RED then GREEN); `fp2pdf.py`'s four hygiene faults fixed to match `fp2dxf.py`'s own precedent (`SystemExit`→`ValueError`, `print()`→a `ConvertResult`, a deferred+guarded `reportlab` import, its own thickness table now read from `fp2dxf.py`'s by-path loader). 23 new tests, full suite + gate GREEN. No AMBER work touched — no new settings keys, no menu wiring, nothing user-visible yet |
| [`0066-ruling.md`](0066-ruling.md) | Ruling — item C, at last: the repair's tolerance is a DISPLACEMENT in inches, not a degree — measured that two nearly-identical angles (0.9094°/0.9290°) move a wall end by 1″ and 3″ respectively, four orders of magnitude spanned within a single "under 1°" band. Sets `T = 1/16″` (32 of 63 corpus walls auto-repairable, boundary items named), finds a per-wall repair is not well-defined (14 of 63 conflict with a shared vertex's own exactly-axis wall) and rules "refuse the conflicted walls and report them" for this first delivery, the acceptance restated as an inequality, never zero. Repair runs only from an explicit menu item, interlocked on the existing invariant checker, one undoable operation. A read-back — the displacement formula's home, the conflict predicate as code, which endpoint moves, the preview's wording, the interlock's check list, the acceptance restated — is owed before the repair itself starts |
| [`0079-report.md`](0079-report.md) | Report — `0066`'s displacement instrument built and gated GREEN, cross-checked against the ruling's own 63-value sorted list to the value (two independent implementations agreeing exactly). The read-back answered in full: the conflict predicate implemented and measured against the corpus — 14 of 63 have at least one conflicted endpoint (matching `0066` exactly), but only 2 have BOTH ends conflicted (fully refused); the other 12 repair through their one free endpoint. Practical count: 61 of 63 repaired, 2 refused, named. The repair itself (item 2) not started — stays AMBER, blocked on this being ruled |
| [`0077-ruling.md`](0077-ruling.md) | Ruling — `0076` accepted, three of four items carry a real revert-and-confirm differential. Finds `coerce_setting`'s own bool branch is `bool(val)` on anything not already a bool — `bool("false") is True`, the exact hazard the whole thread exists to eliminate, alive again in the function whose job is "do not blindly coerce"; a loaded gun, not yet fired, since no string keys exist today. Finds `0076`'s one item with no differential (credited for saying so) and closes it. Finds `fp2pdf.py` now execs the whole of `fp2dxf.py` by path just to borrow seven numbers, needing a `sys.modules` hack for a dataclass it does not use — "the workaround is the evidence the shape is wrong" — orders a shared leaf module instead. Finds a corrupt settings file gets silently emptied on the next write. Finds `reportlab`'s only two real receipts are both skipped in CI, since it is in no requirements file |
| [`0078-ruling.md`](0078-ruling.md) | Ruling — on Patrick's one-line answer ("I want to see the settings in the file"): materialise every `DEFAULT_SETTINGS` key, no exception list. Finds this VOIDS `0074`'s own pinning mitigation (no absent keys left for a future default to fall through to) and rules the fix: `SETTINGS_VERSION` plus an ordered migration table, applied on every load, with a mechanical pin test so editing a default without bumping the version reddens the gate. Corrects a self-contradiction: `0074` tiered materialisation AMBER, `0077` called it GREEN — GREEN is right, since materialisation alone changes no operation's output and nothing on screen |
| [`0080-report.md`](0080-report.md) | Report — every GREEN item across `0077`/`0078` built: the bool branch parses `"true"/"false"/"1"/"0"` explicitly; `reportlab` into `requirements-dev.txt` (its two PDF-rendering receipts now actually run in CI instead of being skipped); the missing differential supplied (reverted a loader call site, confirmed RED, restored); `_stdt.py`, a dependency-free leaf both exporters load by path, `fp2pdf` decoupled from `fp2dxf` entirely (receipted with `fp2dxf.py` genuinely absent from disk); a corrupt settings file quarantined as `.bad` and reported once, not silently emptied, and confirmed migration does not re-run on recovery; full materialisation plus `SETTINGS_VERSION`/the migration table/the pin test, landed together as ordered. 10 new tests, full suite + gate GREEN |
| [`0081-report.md`](0081-report.md) | Report — a consolidated status, not tied to one ruling: three AMBER PRs open and unchanged (#34/#35/#36), all five Patrick checks restated verbatim in one place; item C's read-back answered, the repair itself still blocked; every GREEN settings/`fp2pdf` item across `0072`-`0078` built and merged, everything AMBER/RED there still waiting on `0074`'s own read-back. Nothing currently GREEN and unbuilt — what remains is blocked on Patrick, RED and unruled, or named-not-ordered. `0066` was the only reserved number and it is spent; none is currently reserved |
| [`0082-ruling.md`](0082-ruling.md) | Ruling — accepts `0079`'s read-back with three amendments, two of them self-corrections. Reproduces `0079`'s numbers exactly (14 conflicted, 2 fully refused, the 63-value displacement list) and its own count-of-14-vs-rule-moves-one correction. Withdraws `0066` §5's refuse-to-start clause: measured that all four plans this repair exists for already fail `check()`, so refusing would ship it dead on every one; the differential (`newly_failing = after - before`) is the guarantee that matters, not a clean-start precondition. Finds the conflict predicate goes stale inside its own batch — 34 vertices are shared by two or more near-axis walls, a real six-wall chain among them — and orders it re-evaluated per wall against the document as mutated so far, "61 of 63" downgraded to provisional. Names, does not measure, that the differential must compare on a stable key (invariant code + subject ids) rather than the rendered message, since the repair changes neighbouring walls' lengths. Unblocks item 2 (the repair) to AMBER |
| [`0083-report.md`](0083-report.md) | Report — item C (the repair) built exactly to `0082`'s amended spec, gated GREEN (852 passed, `ruff` clean): `repair_wall_orthogonality` + `OrthogonalityRepairDialog` + Edit ▸ "Repair wall orthogonality…". `0082`'s own chain receipt run on the real plan it named — RED under a naive as-loaded predicate (one wall ends up worse, 3.25° off axis), GREEN under the built one. Two findings neither ruling anticipated, both measured by actually running the built repair against the corpus rather than the two hand-walked files: "61 of 63" does not hold corpus-wide — one intake file alone carries 37 near-axis walls and its whole-file rollback (correct as specified) withholds all of them, honest total 22 moved / 4 refused / 37 withheld (new script: `docs/evidence/orthogonality_repair_census.py`); and a refused wall's own displacement can still change as a side effect of a neighbour's repair through a shared vertex, a real gap in the accepted acceptance clause (f), named for a future ruling rather than silently tested around. AMBER — [PR #37](https://github.com/pjm4github/FloorPlanner/pull/37) open on `wall-orthogonality-repair`, stopped for Patrick's own check (`0066` §7) |
| [`0084-ruling.md`](0084-ruling.md) | Ruling — `T` was wrongly dropped as the candidacy filter (restore it: under it the largest correction is 0.04″, not 3.000″, which is what makes "does the drawing still look like your drawing?" answerable) and the interlock never guarded orthogonality itself (add a post-condition: no wall's deviation may increase — measured `w54` tilted to 4.679° while every existing guard passed). `0083`'s "61 of 63" correction stands but its cause was wrong: `crossfloor-snap` was always in `0066`'s census; the miss was modelling the whole-file rollback. Orders per-wall rollback instead of whole-file, but only after re-measuring under restored `T` — may be moot. Also: the mailbox landed on a feature branch a sixth time, so a pre-commit hook now refuses any new `docs/handoff/NNNN-*.md` off `main` (merge commits exempt) |
| [`0085-ruling.md`](0085-ruling.md) | Ruling — on Patrick's instruction: shorter exchanges (rulings bar at ~3 KB, raw measured tables exempt) and `CLAUDE.md` trimmed to non-obvious traps only (target under 2 KB) — cut content must be moved, never lost, with its destination named per paragraph |
| [`0086-ruling.md`](0086-ruling.md) | Ruling — Patrick checks out branches himself, in the main working tree, no worktree: land the mailbox and the hook work on `main`, bring `main` into the three stale PR branches and re-gate each, leave `HEAD` on `main` clean, then a short ready-report table (branch/tip/verdict/behind/question). `wall-orthogonality-repair` excluded — `0084`'s `T`-restore is one commit from changing what it produces |
| [`0087-report.md`](0087-report.md) | Report — the ready report: all three PRs (#34/#35/#36) brought current with `main`, individually re-gated GREEN, 0 commits behind. One correction: `0086`'s "2/2/1 behind" measured each branch's own unmerged commits, not what was missing from `main` (19/10/7) |
| [`0088-ruling.md`](0088-ruling.md) | Ruling — PR #36 (`t-junction-grid-snap`) PASSED Patrick's check ("the walls snap exactly correct"); merge authorised after bringing `main` in and re-gating; D80 closes on this ruling. `0070`'s other four snap sites stay named, not ordered — one passed check is not a claim about the class |
| [`0089-ruling.md`](0089-ruling.md) | Ruling — PR #34 (`cross-floor-align-fix`) PASSED ("cross floor align problem is fixed"); the grid-snap symptom he also saw is confirmed, measured, as the ABSENCE of #36's fix on a sibling branch, not a regression — disappears once #36 merges and this branch takes `main` in. Merge order: #36 then #34 |
| [`0090-ruling.md`](0090-ruling.md) | Ruling — PR #35 (`wall-label-fixes`) PASSED; all three AMBER checks now discharged in one session, the AMBER queue is empty. Merge sequentially (#36, #34, #35), re-gating the combined tree each time. `0081` item 4 (status-bar label) recorded as answered by inference, correction invited. Orders `0084` §6 (restore `T`, the post-condition, re-measure) and `0085`'s `CLAUDE.md` trim next |
| [`0091-ruling.md`](0091-ruling.md) | Ruling — the status-bar label item closed on Patrick's direct confirmation ("yes the status bar is perfect"), not just inference. `0068` §4 (heading vs. deviation) stays open, still not blocking |
| [`0092-ruling.md`](0092-ruling.md) | Ruling — `0068` §4 answered: "take option 3" — both numbers, heading fixed-decimal and deviation significant-figures (same absolute-vs-relative split already ruled twice). Retires the false-cardinal cliff: a heading can still round to a lie, but the pair together cannot. AMBER, batched with PR #37's check |
| [`0093-ruling.md`](0093-ruling.md) | Ruling — amends `0092`: the angle clause fires only off the nearest 15° (`rotate_snap_deg`), not the nearest cardinal, so a deliberate 45° wall says nothing. Guards against an arbitrary step (must divide 90, else fall back to cardinals-only). The census/report/item C stay cardinal-based, unordered |
| [`0094-ruling.md`](0094-ruling.md) | Ruling — self-correction: walls DO have an angle snap (Ctrl-drag, `_angle_snapped_target`) that `0093` said didn't exist. Measured it does not survive an exact round-trip at 15/30/60/75° (~4e-15° noise) — would clutter the bar on the walls the feature exists to make. Fixes with a 1e-9° suppression tolerance, five orders above the noise and five below the smallest real corpus drift |
| [`0095-report.md`](0095-report.md) | Report — `0084` §6 items 1-3 built: `T` restored as the candidacy filter, the orthogonality post-condition added (a real exemption bug in its own first draft found against `wiscaway`'s `w57` and fixed — no wall is exempted, corpus-wide receipt: zero walls end up worse), the census re-run — the whole-file rollback no longer fires at all, item 3's "change" half unneeded. PR #37 now worth checking. `0092`-`0094` (status-bar angle clause) not yet built |
| [`0096-report.md`](0096-report.md) | Report — `0092`/`0093`/`0094` built: the angle clause fires off the 15° intended-angle grid (not the nearest cardinal), showing both heading and deviation when it does, silently falling back to cardinals-only for an arbitrary `rotate_snap_deg` step. 11 tests, including a 7-way Ctrl-drag-angle-snap receipt that caught the ~4e-15° noise bug before it shipped. AMBER — [PR #38](https://github.com/pjm4github/FloorPlanner/pull/38) open, batched with PR #37's check |
| [`0097-report.md`](0097-report.md) | Report — `CLAUDE.md` trimmed to traps only (`0085` §2): 13,824 → 4,730 bytes, missed the 2 KB target and says why (a few items outside the 12-item KEEP list still pass the "wrong not slow" test). Every cut paragraph moved to the new `docs/ARCHITECTURE.md`, table naming each destination. GREEN, docs-only |
| [`0098-ruling.md`](0098-ruling.md) | Ruling — Patrick selected a wall the report didn't mean: `WallItem.uid` (status bar) and the document id (report) are two different namespaces, both printed as "W<n>", with nothing to tell them apart. Orders endpoints in feet on every named wall ahead of PR #37's check (GREEN); click-to-select and a 15° report grid named, not ordered (RED) |
| [`0099-ruling.md`](0099-ruling.md) | Ruling — `WallItem.uid` is worse than `0098` said: not just a second namespace but non-reproducible across sessions (mints on first read). Also: the check was run against `wiscaway2026-08-08.json`, which has ZERO repairable walls — measured table of which corpus plans do; `wiscaway2026-08-09R` is the one to check. The 104/103 count discrepancy withdrawn — different file (his own OneDrive copy), not the repo's |
| [`0100-ruling.md`](0100-ruling.md) | Ruling — Patrick's own request: matching wall numbers + click-to-highlight, on three surfaces. Document/session ids cannot unify (canonical vs. persistent, load-bearing for the census) — the report shows both. Coalesce needs a real preview it does not have today, not a dialog tweak. Dialogs go modeless. One shared row-list widget, not four. AMBER throughout, in front of PR #37's check. Read-back owed before any code |
| [`0101-report.md`](0101-report.md) | Report — the read-back for `0100` §5, all five questions answered from the code (the doc-id map already exists as an out-param in `bridge._walls_of`; `sip.isdeleted` is the established dead-reference guard, with one real gap named — a merged-but-not-deleted wall; Coalesce's own-item recommendation; gaps dialog needs a vertex-pair variant, not the wall widget unmodified). No code built; waiting on a ruling |
| [`0101-ruling.md`](0101-ruling.md) | Ruling — answers `0100` §5's first read-back item directly: the map is not new machinery, it is two dictionaries (`_walls_of`'s `src` out-param, `canonical.py`'s renumbering) already computed on every report and discarded both times. `design_from_scene`'s existing `report` out-param is the seam to return it through. Hold the `WallItem` itself in a row, not its id — a stale map only matters if the row stores a lookup key instead of the object. GREEN, a finding, not a change |
| [`0102-report.md`](0102-report.md) | Report — `0100` §1 built: `report["wall_items"]` composes the two maps `0101-ruling.md` named; the report's rows now show both ids and the endpoints in feet. Found and fixed, fail-first, a real bug in the natural first draft (the accumulator was per-level like its sibling `of_item`, correct there and silently wrong here — a two-floor plan would have kept only the last floor's walls). AMBER, branch `wall-report-id-fix`, batched with PR #37's check once its own preview gets the same fix |
| [`0103-ruling.md`](0103-ruling.md) | Ruling — `0102` accepted; `0100` §5 vs §6 was a real contradiction, owned, with a new standing rule: naming something as unblocking a person exempts it from its own read-back gate, stated in the same sentence. The four answers: centre (`view.centerOn`) plus select, not select alone; a dead row is the one whose id does not round-trip from a fresh walk, not just `sip.isdeleted`; Coalesce's preview is its own item, RED, pending its own ruling; the gaps dialog is NOT the same treatment (`0100` §2 wrong — a gap is a vertex pair, no wall id), dropped. Orders the shared row-list widget (AMBER) and a one-line duplicate-handoff-number check in the mailbox hook (GREEN); Patrick's check is unchanged and now performable |
| [`0104-ruling.md`](0104-ruling.md) | Ruling — Patrick asked for more agents and less testing. The quick/full split he wants is already built and already in use (`--quick` unlocks a commit, full mode only gates a push); pytest's markers already select. Refuses change-impact test selection — this codebase's star imports, late-import cycle and mutable `SETTINGS` global make "what a feature touches" uncomputable, measured against a real cross-module break `0079` caught. Orders the real cost measured first (how often has ON/DEEP gone RED while OFF was GREEN, before cutting the 3x) and names per-task progress files as the blocker on running agents in parallel — GREEN, nothing parallel starts before it |
| [`0105-ruling.md`](0105-ruling.md) | Ruling — Patrick's two reasons for cutting CI (python-version mismatch, no Qt coverage) both measured false against `ci.yml` — the Windows/py3.13 job is his exact machine, and CI's Qt coverage (offscreen) is identical to local. The reason that survives: one agent, one machine, so CI's usual job (catching what a contributor forgot to run) has no contributor to catch, and no record of CI catching a defect the local gate missed. Orders four jobs cut (disabled, not deleted) — `pytest (py3.10)`, `pytest (windows)`, `pytest deep invariants`, `records (gate --docs)` — keeping one: `pytest (py3.13)` recast as the full local gate, the one run that starts from a clean checkout and can't be bypassed by `--no-verify`. GREEN, a change to the judge, reversible the moment a second agent/machine/person touches the repo |
| [`0106-report.md`](0106-report.md) | Report — `0104` §5 tier 2's ON/DEEP-vs-OFF divergence measured across all 267 CI runs since the workflow began: zero instances of ON or DEEP going RED while OFF was GREEN on a real invariant fault. Every RED was `Docs-Snapshot` staleness (37/47) or `Docs-Refs` unresolved cross-references (7/47) or a base test failure with OFF also red (4/47, ON never ran) — `Gate-DEEP`'s own pytest line read OK in all 37. Corroborates `0105`'s own finding independently. `0105`'s CI cuts built: four jobs disabled (`if: false`, not deleted) with the receipt cited in the file; the surviving job now calls the full local gate; `requires-python` raised to `>=3.13`. Flags one gap `0105` did not cover: `--docs`-specific checks (defect front matter, the index, cross-references, migration dry run) are not part of `tools/gate.py`'s bare/full mode, so cutting the CI docs job means nothing runs them anywhere now, local or CI |
| [`0107-ruling.md`](0107-ruling.md) | Ruling — accepts `0106`'s measurement, then falsifies two sentences of `0105`: the commit hook does NOT run the `--docs` checks (an early, mutually exclusive return in `gate.py`), so cutting CI's docs job removed the only place `Docs-Refs` ever fired (7 of 47 failures) — wire `--docs` into the push hook, live, not a second result file. Also: the 3.10 matrix leg, "disabled not deleted," still spins up a runner and reports green having tested nothing — collapse to `["3.13"]`. And: with zero measured ON/DEEP divergence and the surviving CI job running full mode from a clean, unbypassable checkout, DEEP can leave the developer's own push and run only under CI. GREEN throughout, all three built same session |
| [`0108-ruling.md`](0108-ruling.md) | Ruling — Patrick's own request: "Snap wall to grid," a per-wall manual action, explicitly NOT the orthogonality repair (rounds both ends to grid multiples, not to axis-aligned — a wall can be on-axis and off-grid, or vice versa). `T` does not apply here — it was a proxy for a human, and the human is present, clicking one wall at a time. Four refusals named (degenerate wall, an opening running off, a new invariant violation, a neighbour's angle/grid getting worse) — AMBER, queued behind PR #37/#38 |
| [`0109-ruling.md`](0109-ruling.md) | Ruling — Patrick's walkthrough confirms the arithmetic is plain `wall_snap`, no new function. Rooms recalculate by construction (they derive from the same `Vertex` objects). Amends `0108`'s fourth refusal: a neighbour's angle/grid getting worse is now a REPORT, not a refusal — refusing on it would block nearly every snap in a legacy plan, since that is the exact situation the feature exists for. Format (`fmt_ft2`) unchanged. GREEN to record, feature stays AMBER |
| [`0110-ruling.md`](0110-ruling.md) | Ruling — Patrick's second request, "Snap to Grid Orthogonal": clicked vertex to nearest grid on both coordinates, the other vertex takes the shared axis coordinate. Measured strictly safer than plain Snap to Grid (which can tilt a wall whose ends straddle a grid line) — build this first. The 15°-angle case has no arithmetic solution (`tan 15°` is irrational, so no wall at 15° can have both ends on any grid, at any length) — ship a disabled placeholder menu item with a tooltip explaining why, not a silent no-op. AMBER for the orthogonal snap (first) and plain snap (second, its tilt case now a named limitation), GREEN for the disabled placeholder, RED for the 15° feature itself |
| [`0111-ruling.md`](0111-ruling.md) | Ruling — closes out the open PRs: three, not two — `wall-report-id-fix` needs its own check too (`0102`'s AMBER tier). Orders the session: land or finish `0107`'s in-flight work first; land `0107`-`0110` on `main`, doc-only; clean up the stray `plans.pdf`; bring `main` into all three branches, re-gate each, push; then one check session, `wall-report-id-fix` → PR #37 → PR #38, each check returning to `main` between; merge sequentially, re-gating each merge on the previous; delete branches. Nothing else opens until these three land — `0072` §7's three-concurrent-AMBER-PRs bottleneck has rebuilt itself |
| [`0112-ruling.md`](0112-ruling.md) | Ruling — self-correction: `0111` §2 was wrong that a CLI test wrote `plans.pdf` into the source tree — the test already passes `-o tmp_path/...`; withdrawn, nothing in the tests changes. Gitignore it anyway, for the real reason: `fp2pdf.py`'s `--out` default is a bare relative `plans.pdf`, so any hand-run of the CLI from inside the checkout drops one there — a permanent CLI default, not a one-time test bug. Add the ignore line with its reason, delete the 28KB file on disk. GREEN, folded into `0111` §3's cleanup, not its own commit |
| [`0113-report.md`](0113-report.md) | Report — `0111`'s close-out and `0108`-`0110`'s three snap-to-grid features, all built, checked and merged. PR #39/#37/#38 checked in order and merged (`0111` §4/§5). Snap to Grid Orthogonal (PR #40) and plain Snap to Grid (PR #41) built per `0110` §5's order, both verified against the real corpus before either check ran, both passed Patrick's own check and merged. The disabled 15° placeholder shipped alongside Orthogonal. `main` clean, no open branches or PRs, nothing owed |
| [`0114-ruling.md`](0114-ruling.md) | Ruling — accepts `0113`, spot-checked against the tree. Owes Patrick one confirmation: were the two quoted checks ("Perfect!", "works perfectly") really his, since they happened in Code's session and cannot be verified from this one. The finding: item C's own closing receipt (`0066` §6 — repair the `L2.dxf` source plan, re-export, count what Chief flags against the 75) has never been run; the `wiscaway` 5-wall preview check that merged PR #37 was the correct PRE-merge check, but the FEATURE merging is not the same as item C CLOSING. `0113` §4's "nothing is currently owed" was wrong by exactly this line — the recount is Patrick's alone, no code blocks it. GREEN, a record correction, no new build work ordered |
| [`0115-ruling.md`](0115-ruling.md) | Ruling — Patrick confirms the two `0113`-quoted checks are his own ("yes those are my quotes — duly noted on the Chief import"). `0114` §1 closed; PRs #37/#38/#39/#40/#41's merges stand confirmed, nothing conditional. Still open, his alone: the `L2.dxf` Chief recount (`0066` §6) — no code blocks it, runs whenever Chief is next open, item C closes when he reports the number. GREEN, a record entry, no work |
