# 0131 — ruling: `R2f` screenshot read — every visible defect maps to an item already in flight; nothing new is ordered

**On `fixtures/incoming/wiscaway2026-08-30R2f-.png`** (note the trailing `-` in
the actual filename), Patrick's mid-tranche progress shot.

## 1. WHAT IT SHOWS — a real transformation

The lanes hug the wing, nothing crosses the garage, labels read in
feet-and-inches, the footer carries the openings note. **`DIM45.png` to this is
the [`0127`](0127-ruling.md) fix working.**

## 2. EVERY REMAINING SYMPTOM, MAPPED — Code builds nothing from this file that is not already ordered

| visible in `R2f` | covered by |
|---|---|
| dense tiny segments bottom row (≈ the wing's foot) and left row's crowded run | **[`0129`](0129-ruling.md) §3(a)** grid filter — and the bottom-row bunching is largely wing corners projected into X, which **[`0130`](0130-ruling.md) §1** removes outright |
| fraction labels still present (`6'-2 1/2"`, `2'-2 1/2"`) | **[`0129`](0129-ruling.md) §3(a)** |
| title block still *"overall wall faces"* | **[`0129`](0129-ruling.md) §3(c)** |
| 45° strings near the wing's lower corner brushing the orthogonal row's territory | **watch item, not ordered** — expected to resolve when [`0130`](0130-ruling.md) empties the wing's stations out of the X row; **checked at the re-check, ruled only if it survives** |

**One item outside every ruling, named not ordered:** the FOYER room label
reads `15'-7 1/2" x 8'-0 3/4"` — room-size labels keep fractions. Patrick's
instruction covered *callouts*; **if the labels should round too, that is one
line from him, not an inference.**

## 3. DISPOSITION

`R2f` → `docs/evidence/` beside `DIM45.png` when the tranche lands, cited as
the mid-state. **GREEN — a mapping, no work.** The tranche's check stands as
[`0129`](0129-ruling.md) §4 + [`0130`](0130-ruling.md) §3 wrote it.
