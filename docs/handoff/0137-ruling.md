# 0137 — ruling: the DXF check passes in his words; PR #47 merged clean — and item C's number is one sentence away

**Patrick, 2026‑08‑31:** *"The L1 and L2 dxf layers work fine. Ive asked Code
to merge this version into the main line."*

---

## 1. RECORDED — [`0135`](0135-ruling.md)'s AMBER is discharged

His re-import of both level sheets is the check, and his merge instruction is
the authority. **Measured on `main`: PR #47 merged at `890fafb`, and the
`fp2dxf-door-symbols` ref is gone, local and remote** — the
deletion-folded-into-merge procedure ([`0133`](0133-report.md) §1) held on its
first outing. [`0136`](0136-report.md)'s build stands as reported, including
its §1 finding that the golden fixture itself carried the `"sliding"` bug —
a fixture correction rightly folded into the fix it belonged to.

**The `FP-WINDOWS` mapping question ([`0135`](0135-ruling.md) §3) goes moot
with the pass** — unless the windows in fact did *not* appear this time, in
which case one word reopens it.

## 2. THE ONE NUMBER STILL OWED — item C, open since [`0066`](0066-ruling.md)

"Work fine" closes this tranche. It does not yet close the board's oldest
line, because that receipt was ruled as a **count, not a verdict**
([`0066`](0066-ruling.md) §7):

> *How many walls does Chief flag as off-axis now, against the 75 before?*

**Patrick: while this import is still on screen — does Chief flag any off-axis
walls on L1/L2, and roughly how many?** Zero-or-near closes item C after a
month open; anything else tells us the repair's residue is visible in Chief
and gets its own look. Either way it is one sentence, and this Chief session
is the sitting [`0066`](0066-ruling.md) asked for.

## 3. TIER

**GREEN — record only.** Nothing new is ordered; Code has nothing to build
from this file.

**Carried:** item C's count (§2, Patrick, one line); [`0134`](0134-report.md)'s
PDF-tranche check still unreported (PR #46 merged — one word if the cleaned-up
PDF looked right); room-label rounding ([`0131`](0131-ruling.md) §2);
delta-snap sites; D61-family.
